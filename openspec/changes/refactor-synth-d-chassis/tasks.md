## 1. Establish the parameter-schema module

- [x] 1.1 Create `src/param-schema.js` exporting an ordered map of parameter descriptors `{ min, max, default, bipolar, kind: 'knob' | 'switch' | 'controller', ccScalable: boolean }`, populated from the current `CONTINUOUS_DEFAULTS`/`SWITCH_DEFAULTS` (state) and `KNOB_PARAMS`/`BIPOLAR_PARAMS` (App.svelte) values. Per D2/D6: `keyTrack` is `kind: 'switch'` with `ccScalable: true`; `modWheel` is `kind: 'controller'` with `ccScalable: true` (it has no store default and must NOT appear in store derivations); pure switches are `ccScalable: false`
- [x] 1.2 Derive and export from the schema module: store membership `PARAM_DEFAULTS`/`PARAM_NAMES`/`AUDIO_PARAMS` = descriptors with `kind ∈ {knob, switch}` (controllers excluded); `KNOB_PARAMS` = descriptors with `ccScalable === true`; `BIPOLAR_PARAMS` = descriptors with `bipolar === true`; `powerOffValue` = bipolar → midpoint, else → min
- [x] 1.3 Add a unit test asserting the schema-derived `PARAM_DEFAULTS`, `PARAM_NAMES`, `AUDIO_PARAMS`, `KNOB_PARAMS`, `BIPOLAR_PARAMS`, and `powerOffValue` outputs exactly equal the pre-refactor literal values — explicitly asserting `modWheel` is absent from `PARAM_DEFAULTS`/`PARAM_NAMES`/`AUDIO_PARAMS` and present in `KNOB_PARAMS` (the equality gate before deleting originals)

## 2. Make the store derive from the schema

- [x] 2.1 Replace `CONTINUOUS_DEFAULTS`/`SWITCH_DEFAULTS` in `src/state/synth.svelte.js` with the schema-derived `PARAM_DEFAULTS`/`PARAM_NAMES`/`AUDIO_PARAMS`, and have the store **re-export** those three so existing importers keep their current `./state/synth.svelte.js` path (no churn for `App.svelte`, `synth.test.js`, or `patches/storage.js`); keep all store machinery (`writeParam`, `applyParams`, `resetParams`, `resetToDefaults`, `serializeParams`, `setActivePatch`) unchanged. This removes the `CONTINUOUS_DEFAULTS`/`SWITCH_DEFAULTS` literals
- [x] 2.2 Confirm existing importers of `PARAM_DEFAULTS`/`PARAM_NAMES`/`AUDIO_PARAMS` (`App.svelte`, `synth.test.js`, `patches/storage.js`) still resolve via the store's re-export; no import-path change needed there, assertions unchanged
- [x] 2.3 Grep for `CONTINUOUS_DEFAULTS` and `SWITCH_DEFAULTS`; confirm zero remaining references (the literals are now fully removed) before proceeding
- [x] 2.4 Run `npx vitest run` for the store and patches tests; confirm green with no assertion changes

## 3. Make the UI knob metadata derive from the schema

- [x] 3.1 Remove the `KNOB_PARAMS`/`BIPOLAR_PARAMS`/`powerOffValue` block from `App.svelte`'s `<script module>` and import the schema-derived equivalents. This removes those literals
- [x] 3.2 Update `App.test.js`'s import of `powerOffValue` to the schema module path — the only external importer (`KNOB_PARAMS` is module-private; `BIPOLAR_PARAMS` has no external test importer); assertions unchanged
- [x] 3.3 Grep for `KNOB_PARAMS`/`BIPOLAR_PARAMS`/`powerOffValue`; confirm the `App.svelte` block is gone and the only references are the schema-derived definitions and updated imports, before proceeding
- [x] 3.4 Run the affected component tests; confirm green

## 4. Split App.svelte into Shell + instrument panel layout

- [x] 4.1 Create `src/components/Shell.svelte` owning the instrument-independent frame: header/title/version, GitHub link, power button + `handleToggle` lifecycle, MIDI manager wiring + `MidiStatus`, `PatchControl`, keyboard row (`Keyboard` + `WheelsPanel` + `RegisterPanel`, with `activeRegister`/`keyboardBase` as Shell-internal state), `Scope`, store seeding, and the param/wheel change chokepoints; expose the param-name-agnostic `midiStateFor` function (NOT pre-computed per-panel `$derived` state) plus the D3 contract callbacks; render instrument content through a passthrough that hands the contract data to its children, i.e. `{@render children({ midiStateFor, onParamChange, onKnobContextMenu, powered, getMixerPeak, getOutputPeak })}`, with no instrument imports. In `App.svelte`, wrap the **existing inline panel markup** in the Shell's children snippet and destructure those props, so the markup still resolves `midiStateFor`/`powered`/peak readbacks (which left App's script scope) and the app renders unchanged at this commit. Chassis CC-scaling and power-off logic in the Shell import `KNOB_PARAMS` and `powerOffValue` directly from `src/param-schema.js` (the sanctioned cross-tier import, see D1). (This passthrough lets 4.1 and 4.2 be separate, always-rendering commits — avoiding both a non-rendering intermediate and an oversized combined commit.)
- [x] 4.2 Extract the panel markup into `src/components/SubtractivePanels.svelte` (the `<div class="panels">` grid and filter/output grid: Oscillator, Mixer, Filter, AmpEnv, Effects, Modulation, Glide), moving the per-panel `$derived` MIDI-state blocks with it so they call the Shell's `midiStateFor` with the instrument's own param names (all instrument param-name literals on the instrument side per D4); it reads the discrete `synthParams.*` switch values and consumes the Shell callbacks (`onParamChange`, `onKnobContextMenu`), meter readbacks, and `powered`. `App.svelte` now passes `<SubtractivePanels/>` as the Shell's child — still rendering at this commit
- [x] 4.3 Confirm `src/App.svelte` is now a thin composition (`Shell` wrapping `SubtractivePanels`) with no residual frame or panel logic
- [x] 4.4 Inventory `App.svelte`'s `<style>` for `:global` rules and inherited custom properties (e.g. `--knob-body-size` on `.app`) before splitting; move each rule to whichever of Shell/panels owns its markup and keep shared custom properties on the Shell root. Verify pixel-identity concretely: capture a Playwright `page.screenshot()` at the power-off and power-on states before the split and assert the post-split render matches via `toMatchSnapshot()` with a small pixel threshold; these baselines are a throwaway refactor gate, discarded after the change merges (not a permanent test). Update `App.test.js` and add/relocate a `Shell.test.js` as needed (assertions describing existing behavior unchanged)
- [x] 4.5 Run `npx vitest run` (full unit + component suite); confirm green with assertion changes limited to import paths

## 5. Enforce and verify the chassis seam

- [x] 5.0 (added at implementation, human-approved) Relocate the instrument-specific CC rename table out of the chassis: move `PARAM_RENAMES` (`reverbMix → reverbSend`) from `audio/midiCcMap.js` into the instrument-owned `param-schema.js`, make `MidiCcMap` param-name-agnostic by taking the rename table via its constructor (default none), and have the `Shell` inject it via `new MidiCcMap(PARAM_RENAMES)` (importing `PARAM_RENAMES` from the schema — the D1 sanctioned import). Update the four `midiCcMap.test.js` rename tests to pass the table explicitly. This is what lets 5.1 pass truthfully rather than via an allowlist (see design.md D4)
- [x] 5.1 Add a chassis-purity vitest test that reads the schema's parameter names (excluding the five universal engine params `freq`/`gate`/`modWheel`/`mixerPeak`/`outputPeak`) and scans the chassis source files (audio engine, MIDI, keyboard, patch storage, store machinery, `Shell`, generic components — but NOT `SubtractivePanels`, which is the instrument layout) for any of those instrument-specific names. Match names as word-bounded JS identifiers (e.g. `\bcutoff\b`, in property-access/quoted-key/template-literal positions) in non-comment code, with an explicit allowlist for any benign common-word collision that arises — so `cutoff`/`resonance` don't false-positive on comments or CSS. Start with this word-bounded approach; escalate to AST-level string-literal/property-access analysis only if it proves unworkable — do not build an AST scanner on the first pass. Because the per-panel `$derived`/`midiStateFor` invocations live in `SubtractivePanels` (D4), the `Shell` carries no instrument param-name literals and the test passes without an exception. Deriving the blocklist from the schema keeps the test self-updating rather than hand-maintained
- [x] 5.2 Verify `Shell.svelte` imports no Tier-3 instrument module **except `src/param-schema.js`** (the sanctioned cross-tier contract import per D1) — i.e. no import of Oscillator/Mixer/Filter/AmpEnv/Effects/Modulation/Glide/`filterGains`/`Volume`/`SubtractivePanels`
- [x] 5.3 Final residual sweep: the original literal tables were already removed in 2.1 (`CONTINUOUS_DEFAULTS`/`SWITCH_DEFAULTS`) and 3.1 (`KNOB_PARAMS`/`BIPOLAR_PARAMS`/inline `powerOffValue`); grep all five symbol names across source and tests to confirm zero residual references remain

## 6. Full regression gate

- [ ] 6.1 Run `npx vitest run` — all unit + component tests pass
- [ ] 6.2 Run `npx stryker run` — mutation score ≥ 85%
- [ ] 6.3 Run `npx playwright test` — all E2E pass
- [ ] 6.4 Confirm the only test edits across the change were import-path updates (no weakened or altered assertions); if any assertion had to change, treat as a regression and fix the code
