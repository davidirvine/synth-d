## 1. CSS Fix

- [x] 1.1 Add `width: fit-content` to the `.app` rule in `src/App.svelte`

## 2. Verification

- [x] 2.1 Open the app in a browser and confirm the header no longer stretches when the window is resized
- [x] 2.2 Confirm the left and right header overhangs over the panel grid are visually equal
- [x] 2.3 Run `npx vitest run` and confirm all tests pass

## 3. Linting and Formatting

- [x] 3.1 Run `npx eslint --fix src/App.svelte` and resolve any findings
- [x] 3.2 Run `npx prettier --write src/App.svelte`
