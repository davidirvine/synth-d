## 1. EmptyPanel Component

- [x] 1.1 Create `src/components/EmptyPanel.svelte` with a `<div class="panel">` and matching panel styles (`background: #1c1c1c`, `border: 1px solid #333`, `padding: 10px 12px`)

## 2. App Integration

- [x] 2.1 Import `EmptyPanel` in `src/App.svelte` and render it after `<Scope>` inside `filter-output-grid`

## 3. Tests

- [x] 3.1 Add `src/components/EmptyPanel.test.js` — verify the component mounts and renders a `.panel` div with no child elements
