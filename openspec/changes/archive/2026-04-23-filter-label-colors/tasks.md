## 1. Style Fix

- [x] 1.1 In `src/components/Knob.svelte`, change `.tick-label { fill }` from `#555` to `#e8dcc8`

## 2. Lint & Format

- [x] 2.1 Run `npx eslint --fix src/components/Knob.svelte`
- [x] 2.2 Run `npx prettier --write src/components/Knob.svelte`

## 3. Verification

- [x] 3.1 Run `npx vitest run` and confirm all tests pass
